apiVersion: v1
kind: ConfigMap
metadata:
  name: demo-app-config
  labels:
    app: demo-app
    environment: stage
data:
  APP_NAME: demo-app
  ENVIRONMENT: stage
  MESSAGE: Day 2 ApplicationSet deployment for stage (manual sync)
