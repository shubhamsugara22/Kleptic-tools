apiVersion: v1
kind: Service
metadata:
  name: { { include "kleptic-app.fullname" . } }
  labels: { { - include "kleptic-app.labels" . | nindent 4 } }
spec:
  type: { { .Values.service.type } }
  ports:
    - port: { { .Values.service.port } }
      targetPort: http
      protocol: TCP
      name: http
  selector: { { - include "kleptic-app.selectorLabels" . | nindent 4 } }
